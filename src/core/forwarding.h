#pragma once

// Forward declarations
class HftClassifier;
// Forward declarations
class HftClassifier;

// Renamed to reflect its purpose: strictly// Main forwarding loop (Core 0)
void lcore_forward_loop(HftClassifier &classifier);